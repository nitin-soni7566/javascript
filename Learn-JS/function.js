

    function add(x, y) {   // creating a function
      return a + b;
    }

    let a = 5;
    let b = 2;

    console.log("addtion of a and b is : " + add(a, b)); // function calling or invoke

    // new way to creating a function 

    const sum = (i, j) => {
      return i + j / 2;
    }




